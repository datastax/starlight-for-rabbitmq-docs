= Consistent metadata store
:navtitle: Metadata store

{product} uses {zookeeper-reg} to store the AMQP entities metadata consistently.
The existing {zookeeper-short} configuration store can be reused to store the {product} metadata.
{product} employs the `/starlight-rabbitmq` prefix to write its entries in {zookeeper-short}.